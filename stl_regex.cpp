#include <regex>
#include <iostream>

using std::regex;

regex pat {R"(\w{2})"};

// void test() 
// {
//     int lineno = 0;
//     for (string line; getline(cin, line); ) {
//         ++ lineno;
//         smatch matches; 
//         if (regex_search(line,matches,pat)
//             std::cout << lineno << ": " << matches[0] << '\n';
//     }
// }

int main()
{
    // regex_match();
    // regex_search();
    // regex_replace();

}